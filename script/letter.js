document.addEventListener("DOMContentLoaded", () => {
  // Ensure the letter is in its default closed state
  const letterCard = document.getElementById("letter-card");
  letterCard.classList.remove("open");
});
